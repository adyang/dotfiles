---
name: dotfiles
on: [push]
jobs:
  test:
    strategy:
      matrix:
        os: [macos-12, macos-11]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
      - name: Test Spotlight Configration
        run: >
          ./configure-macos
